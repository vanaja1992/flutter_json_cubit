import 'package:flutter/material.dart';

class AppColors{
  static Color primaryColor = Colors.white;   //#2B65EC (Ocean Blue) HTML Color Code.
  static Color buttonColor = const Color(0XFF2B65EC);  //0X for hexa code  00 - for 0 brightness  FF -for high brightness after that color code
  static Color iconCheckColor = Colors.green.withOpacity(.3);
}
